---
phase: quick
plan: 260411-1xg
type: execute
wave: 1
depends_on: []
files_modified:
  - task-board.jsx
autonomous: true
must_haves:
  truths:
    - "Landing page header shows today's date and day of week in Chinese"
    - "Status board table headers show actual task names instead of '任务1', '任务2'"
    - "Admin tasks tab has a dedicated save button for the announcement textarea"
    - "Penalty text on Landing StatusBoard and MemberView is prefixed with '惩罚：'"
    - "CountDown component shows remaining time in days (e.g., '3天') instead of hours/minutes"
  artifacts:
    - path: "task-board.jsx"
      provides: "All 5 UI changes"
---

<objective>
Apply 5 UI polish changes to the task board: date display in header, real task names in status table, separate announcement save button, penalty prefix label, and day-based remaining time.

Purpose: Improve readability and usability of the weekly task board for Chinese-speaking team members.
Output: Updated task-board.jsx with all changes.
</objective>

<context>
@task-board.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Date display, task names in status table, penalty prefix, countdown in days</name>
  <files>task-board.jsx</files>
  <action>
Make these 4 changes in task-board.jsx:

1. **Landing header date display (line ~170-193 area):**
   After the "选择你的名字进入" paragraph inside the Landing header div, add a new paragraph showing today's date and day of week. Use:
   ```jsx
   <p style={{
     fontFamily: "'Albert Sans', sans-serif",
     fontSize: "0.8125rem",
     color: c.textMuted,
     margin: "8px 0 0",
     fontVariantNumeric: "tabular-nums",
   }}>
     {(() => {
       const d = new Date();
       const days = ["日", "一", "二", "三", "四", "五", "六"];
       return `${d.getFullYear()}年${d.getMonth() + 1}月${d.getDate()}日 星期${days[d.getDay()]}`;
     })()}
   </p>
   ```

2. **Status table headers — show task names (line ~387):**
   In the StatusBoard component, change the table header from:
   ```jsx
   <th key={t.id} style={{ ...s.th, textAlign: "center" }}>任务{idx + 1}</th>
   ```
   to:
   ```jsx
   <th key={t.id} style={{ ...s.th, textAlign: "center", maxWidth: 80, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{t.name}</th>
   ```

3. **Penalty prefix — add "惩罚：" in two locations:**
   - StatusBoard component (line ~327-329): Change `{week.penalty}` to `{"惩罚：" + week.penalty}`
   - MemberView component (line ~804-805): Change `{week.penalty}` to `{"惩罚：" + week.penalty}`

4. **CountDown component (lines ~456-476):**
   Replace the hours/minutes display with days. Change the calculation and return:
   ```jsx
   const days = Math.ceil(diff / 86400000);
   return (
     <span style={{
       marginLeft: 8,
       color: days <= 1 ? c.danger : c.warning,
       fontWeight: 600,
     }}>
       剩余 {days}天
     </span>
   );
   ```
   Remove the existing `hours` and `mins` const declarations.
  </action>
  <verify>
    <automated>cd /Users/jimmymacmini/Desktop/ai-project/bank-weekly-task && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Landing header shows formatted Chinese date with day of week
    - Status table columns show actual task names (truncated with ellipsis if too long)
    - Penalty text shows "惩罚：" prefix in both StatusBoard and MemberView
    - CountDown shows "剩余 N天" format instead of hours/minutes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add separate save button for weekly announcement in admin panel</name>
  <files>task-board.jsx</files>
  <action>
In the AdminView component, add a dedicated save button for the announcement section.

In the admin "tasks" tab (line ~617-625 area), after the textarea for announcement and before the closing `</div>` of that card, add a save button that calls api.updateAnnouncement directly:

```jsx
<button style={{
  ...s.btnPrimary,
  marginTop: 8,
  background: c.warning,
}} onClick={async () => {
  try {
    await api.updateAnnouncement(announcement);
    await refreshData();
    alert("通知已保存");
  } catch (e) { console.error("Save announcement failed:", e); }
}}>保存通知</button>
```

This gives the admin a way to save the announcement independently from the deadline/penalty settings which are saved via the existing publishWeek flow.
  </action>
  <verify>
    <automated>cd /Users/jimmymacmini/Desktop/ai-project/bank-weekly-task && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Admin tasks tab shows a "保存通知" button directly below the announcement textarea
    - Clicking it saves the announcement via the existing API endpoint and refreshes data
  </done>
</task>

</tasks>

<verification>
- `npx vite build` completes without errors
- Visual check: Landing page shows date, status table shows task names, penalty shows prefix, countdown shows days, admin has announcement save button
</verification>

<success_criteria>
All 5 UI changes are applied to task-board.jsx and the app builds successfully.
</success_criteria>

<output>
No summary file needed for quick tasks.
</output>
