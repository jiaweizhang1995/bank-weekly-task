---
phase: quick-260411-d9t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - task-board.jsx
autonomous: true
requirements:
  - QUICK-260411-d9t
must_haves:
  truths:
    - "On the Landing view, the ☰ menu button is vertically centered with the h1 '周工作看板' title (not aligned to the top of the header next to the '平安银行顶私顾问' subtitle)."
    - "The subtitle '平安银行顶私顾问' still appears above the h1 title."
    - "The date paragraph still appears below the h1 title."
    - "Clicking ☰ still opens the dropdown panel anchored to the menu button (top: 52, right: 0 relative to its wrapper)."
    - "The dropdown still contains '管理后台' and '导出为长图' entries that work as before."
    - "html2canvas export of the board still excludes the menu button (data-html2canvas-ignore preserved)."
  artifacts:
    - path: "task-board.jsx"
      provides: "Restructured Landing header where h1 + menu button share a flex row"
      contains: "justifyContent"
  key_links:
    - from: "h1 周工作看板"
      to: "☰ menu wrapper"
      via: "flex row container with alignItems: center, justifyContent: space-between"
      pattern: "alignItems.*center"
    - from: "menu wrapper"
      to: "dropdown panel"
      via: "position: relative anchor for top:52/right:0 panel"
      pattern: "position.*relative"
---

<objective>
Align the ☰ menu button on the Landing view header so it is vertically centered with the h1 "周工作看板" title, instead of being absolutely positioned at the top-right of the header (currently visually aligning with the "平安银行顶私顾问" subtitle).

Purpose: Improve visual balance of the Landing header — the menu trigger should sit on the same baseline as the dominant title, not on the small uppercase subtitle.

Output: Updated `task-board.jsx` Landing header block (~lines 354–453) with a flex row wrapping the h1 + menu button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@task-board.jsx

<interfaces>
<!-- Current Landing header structure (task-board.jsx lines 354–453). -->
<!-- Outer wrapper: position: relative, padding "40px 24px 32px", borderBottom. -->
<!-- Menu wrapper: position: absolute, top: 20, right: 20, data-html2canvas-ignore="true". -->
<!--   - <button> ☰ with style s.headerMenuButton -->
<!--   - When menuOpen: backdrop button + panel with style {...s.headerMenuPanel, top: 52, right: 0} -->
<!-- Subtitle: <p>平安银行顶私顾问</p> (uppercase, accent color, margin 0 0 8px) -->
<!-- Title: <h1>周工作看板</h1> (clamp font-size, lineHeight 1.15, margin 0) -->
<!-- Date: <p>{YYYY年M月D日 星期X}</p> (margin 10px 0 0) -->

<!-- Reused style tokens (do not modify): -->
<!-- s.headerMenuButton, s.headerMenuBackdrop, s.headerMenuPanel, s.headerMenuItem -->
<!-- c.border, c.accent, c.text, c.textMuted, c.surfaceAlt, c.textFaint -->

<!-- The separate `Header` component (around lines 1315–1398) is OUT OF SCOPE — already uses flex. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Landing header so ☰ menu button shares a flex row with the h1 title</name>
  <files>task-board.jsx</files>
  <action>
In `task-board.jsx`, modify the Landing view header block (currently around lines 354–453, the `<div ref={captureRef}>` containing the absolutely-positioned menu wrapper, subtitle, h1, and date).

Make these changes (and ONLY these changes — do not touch any other component, do not modify the separate `Header` component near lines 1315–1398, do not modify the `s` style object, do not modify any color tokens):

1. **Remove absolute positioning from the outer header div.** The wrapper `<div style={{ padding: "40px 24px 32px", textAlign: "left", borderBottom: ..., position: "relative" }}>` no longer needs `position: "relative"` because the menu wrapper will no longer be absolutely positioned. You may keep it or remove it — preference: remove it for cleanliness.

2. **Reorder children to: subtitle → (h1 + menu row) → date.**
   - Keep the subtitle `<p>平安银行顶私顾问</p>` exactly as-is, as the FIRST child of the header div.
   - Replace the next sibling with a NEW flex row container `<div>` that holds the h1 and the menu wrapper side-by-side:
     ```jsx
     <div style={{
       display: "flex",
       alignItems: "center",
       justifyContent: "space-between",
       gap: 16,
     }}>
       <h1 style={{
         fontFamily: "'Bricolage Grotesque', serif",
         fontSize: "clamp(1.75rem, 5vw + 0.5rem, 2.25rem)",
         fontWeight: 800,
         color: c.text,
         margin: 0,
         lineHeight: 1.15,
       }}>周工作看板</h1>
       <div
         data-html2canvas-ignore="true"
         style={{
           position: "relative",
           flexShrink: 0,
         }}
       >
         {/* existing ☰ button + menuOpen backdrop + panel — UNCHANGED */}
       </div>
     </div>
     ```
   - Inside the menu wrapper `<div>`, keep the existing children EXACTLY as they are today: the `<button>` with `style={s.headerMenuButton}`, the conditional `{menuOpen && (<>...</>)}` block containing the backdrop button and the panel `<div style={{ ...s.headerMenuPanel, top: 52, right: 0 }}>` with its two menu items (管理后台 + 导出为长图). Do NOT change handlers, aria attributes, disabled logic, hover handlers, or panel offsets.
   - Keep the date `<p>` exactly as-is, as the LAST child of the header div, AFTER the new flex row.

3. **Critical constraints:**
   - The menu wrapper MUST keep `data-html2canvas-ignore="true"` so html2canvas-pro export still hides it (per Quick 260411-2hv).
   - The menu wrapper MUST become `position: "relative"` (replacing the old `position: "absolute", top: 20, right: 20`) so the dropdown panel — which uses `top: 52, right: 0` — remains anchored to the button. Do NOT change `top: 52, right: 0` on the panel.
   - Add `flexShrink: 0` to the menu wrapper so the button never shrinks when the h1 wraps on narrow screens.
   - Do NOT introduce CSS files, classes, or styled-components — this file uses inline style objects only.
   - Do NOT change any other view (Member, Admin, Landing's status board, footer, etc.).
   - Do NOT touch `src/main.jsx`, `src/index.css`, or `vite.config.js`.

4. **Visual sanity:** After the change, the header reads top-to-bottom as:
   - `平安银行顶私顾问` (small uppercase, accent color)
   - `周工作看板`        ☰   (h1 left, menu button right, vertically centered)
   - `2026年4月11日 星期六` (small muted, below)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const src=fs.readFileSync('task-board.jsx','utf8');const start=src.indexOf('captureRef');const block=src.slice(start,start+3500);if(!block.includes('justifyContent'))throw new Error('missing flex justifyContent in Landing header');if(!block.includes('alignItems'))throw new Error('missing alignItems in Landing header');if(!block.includes('data-html2canvas-ignore'))throw new Error('lost data-html2canvas-ignore on menu wrapper');if(!block.includes('top: 52'))throw new Error('lost panel top:52 anchor');if(block.includes('top: 20')&&block.includes('right: 20'))throw new Error('still using absolute top:20 right:20 — restructure not applied');console.log('OK');"</automated>
  </verify>
  <done>
    - `task-board.jsx` Landing header (around lines 354–453) restructured per action steps.
    - Header renders subtitle, then a flex row containing h1 + ☰ menu button (vertically centered), then the date.
    - Menu wrapper uses `position: relative` with `data-html2canvas-ignore="true"` preserved.
    - Dropdown panel still anchored at `top: 52, right: 0` and both menu items (管理后台, 导出为长图) still function.
    - `npm run build` (or `npm run dev`) succeeds with no syntax errors.
    - No other files modified.
  </done>
</task>

</tasks>

<verification>
- Open the app, confirm Landing view: ☰ button is vertically centered with the "周工作看板" title (not floating up by the subtitle).
- Click ☰ → dropdown opens just under the button, anchored to its right edge.
- Click "管理后台" → admin login flow opens (existing behavior).
- Click "导出为长图" → existing export flow runs; verify generated long image does NOT contain the ☰ button (html2canvas-ignore preserved).
- Resize to narrow viewport (~360px): h1 stays on the left, ☰ stays on the right, button does not shrink, layout does not break.
</verification>

<success_criteria>
- ☰ menu button is vertically centered with the h1 "周工作看板" title on the Landing view.
- All existing menu functionality (open/close, 管理后台, 导出为长图) works unchanged.
- html2canvas export still excludes the menu button.
- Build succeeds; no other views or components touched.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-d9t-align-menu-header/260411-d9t-SUMMARY.md`
</output>
