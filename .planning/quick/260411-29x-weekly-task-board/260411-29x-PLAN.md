---
phase: quick
plan: 260411-29x
type: execute
wave: 1
depends_on: []
files_modified:
  - task-board.jsx
autonomous: true
must_haves:
  truths:
    - "首页顶部英文标题显示为“平安银行顶私顾问周看板”"
    - "首页周工作看板标题下方不再显示“选择你的名字进入”"
    - "管理员在设置区点击“保存设置”后，界面中央会出现“保存成功”的反馈提示"
  artifacts:
    - path: "task-board.jsx"
      provides: "首页文案调整与管理后台保存成功居中提示"
---

<objective>
Apply the requested weekly task board copy updates and add a centered success confirmation for the admin settings save action.

Purpose: Align the landing page branding with the requested bank-specific title and give admins immediate visual feedback after saving settings.
Output: Updated task-board.jsx implementing the new landing copy and the centered “保存成功” success prompt.
</objective>

<context>
@.planning/STATE.md
@task-board.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update landing-page title copy and remove the entry hint text</name>
  <files>task-board.jsx</files>
  <action>
In the Landing component header, replace the small uppercase English label text `Weekly Task Board` with `平安银行顶私顾问周看板`. Keep the existing visual style unless a minor font-size adjustment is needed to prevent overflow.

Remove the paragraph under the main `周工作看板` heading that currently says `选择你的名字进入`, without leaving an empty gap. Preserve the date line below it so the header spacing still looks intentional after the text is removed.
  </action>
  <verify>
    <automated>cd /Users/jimmymacmini/Desktop/ai-project/bank-weekly-task && npx vite build</automated>
  </verify>
  <done>
    - Landing header label reads `平安银行顶私顾问周看板`
    - The `选择你的名字进入` helper text is removed
    - Header spacing remains visually consistent
  </done>
</task>

<task type="auto">
  <name>Task 2: Add centered success feedback for the admin “保存设置” action</name>
  <files>task-board.jsx</files>
  <action>
In AdminView, add local UI state for a transient save-success message tied specifically to the existing `保存设置` button in the `截止时间与规则` card. After `publishWeek` successfully completes its API updates and `refreshData()` call, show a centered overlay, toast, or floating message with the exact text `保存成功`.

The feedback must appear in the visual center of the current interface, not as a browser `alert()` and not inline under the button. Make it auto-dismiss after a short delay and ensure failures do not show the success prompt. Keep the implementation self-contained in `task-board.jsx`, following the file’s existing inline-style pattern.
  </action>
  <verify>
    <automated>cd /Users/jimmymacmini/Desktop/ai-project/bank-weekly-task && npx vite build</automated>
  </verify>
  <done>
    - Clicking `保存设置` with a successful request shows a centered `保存成功` prompt
    - The prompt disappears automatically after a short delay
    - No browser alert is used for this flow
  </done>
</task>

</tasks>

<verification>
- `npx vite build` completes successfully
- Visual check: landing page shows the new bank-specific title and no longer shows `选择你的名字进入`
- Visual check: admin `保存设置` shows a centered `保存成功` prompt after a successful save
</verification>

<success_criteria>
The quick task plan fully covers the requested title update, helper-text removal, and centered save-success feedback in a single autonomous execution plan.
</success_criteria>

<output>
No summary file needed for quick tasks.
</output>
